{
  "name": "@cas/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "CaS CLI — interact with CLI as a Service from the terminal",
  "bin": {
    "cas": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "pnpm build"
  },
  "dependencies": {
    "commander": "^13.0.0",
    "chalk": "^5.4.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.8.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
